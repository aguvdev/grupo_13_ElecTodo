<!DOCTYPE html>
<html lang="es">

<%- include('../partials/head') %>

  <body>
    <!-- Main -->
    <main class="grilla-login">
      <div class="cajacon"></div><!-- Div para el before del circulo del fondo -->
      <div class="icono-electodo">
        <a href="/">
          <img src="/img/electodo log.png" alt="">
        </a>
      </div>
      <div class="login">
        <h2 class="title">Iniciar Sesion</h2>
        <form action="/users/login" class="formulario" id="formulario" method="POST">
          <!-- Grupo: Correo Electronico -->
          <div class="formulario__grupo" id="grupo__correoL">
            <div class="formulario__grupo-input">
              <i class="fas fa-at"></i>
              <input type="email" class="formulario__input <%= locals.errors && errors.email ? 'is-invalid' : null%>"
                value="<%= locals.oldData ? oldData.email : null%>" name="email" id="emailL"
                placeholder="Escriba su correo.">
              <i id="check-correoL" class="formulario__validacion-estado fas fa-times-circle"></i>
            </div>
            <% if (locals.errors && errors.email) { %>
              <div class="text-danger">
                <%= errors.email.msg%>
              </div>
              <% } %>
                <p id="formulario__input-error-correoL" class="formulario__input-error">No es un correo válido.</p>
          </div>
          <!-- Grupo: Contraseña -->
          <div class="formulario__grupo" id="grupo__passwordL">
            <div class="formulario__grupo-input">
              <i class="fas fa-lock"></i>
              <input type="password"
                class="formulario__input <%= locals.errors && errors.password ? 'is-invalid' : null%>" name="password"
                id="passwordL" placeholder="Escriba su contraseña">
              <i id="check-passwordL" class="formulario__validacion-estado fas fa-times-circle"></i>
              <span class="mostrarPass" onclick="togglePass()">
                <i class="fa fa-eye-slash"></i>
              </span>
            </div>
            <% if (locals.errors && errors.password) { %>
              <div class="text-danger">
                <%= errors.password.msg %>
              </div>
              <% } %>
                <p id="formulario__input-error-passwordL" class="formulario__input-error">La contraseña tiene que tener 8 digitos a 16.</p>
          </div>
          <div class="mb-3 form-check">
            <input type="checkbox" class="form-check-input" id="recordarmeL" name="recordar">
            <label class="form-check-label" for="exampleCheck1">Recordarme</label>
          </div>

          <div class="formulario__mensaje" id="formulario__mensaje">
            <p><i class="fas fa-exclamation-triangle"></i> <b>Error:</b> Por favor rellena los campos correctamente. </p>
          </div>

          <div class="formulario__grupo formulario__grupo-btn-enviar">
            <input type="submit" id='btn1' value="Enviar" class="formulario__btn" />
          </div>
        </form>
      </div>
      <!-- Lado Izquierdo: Ir a Register -->
      <div class="panels-container">
        <div class="panel left-panel">
          <div class="content">
            <h3>¿Nuevo?</h3>
            <p>
              ¡Dale al botón para registrarte de una manera sencilla!
            </p>
            <a href="/users/register">
              <button class="btn transparent" id="sign-up-btn">
                Registrarse
              </button>
            </a>
          </div>
          <img src="/img/login.svg" class="image" alt="" />
        </div>
      </div>
    </main>
    <%- include('../partials/footer') %>
      <%- include('../partials/scripts') %>
  </body>

</html>