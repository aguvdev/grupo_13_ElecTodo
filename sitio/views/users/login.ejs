<!DOCTYPE html>
<html lang="es">

<%- include('../partials/head') %>

<body class="login">
  <%- include('../partials/header')  %>
    <main>
        <div class="row">
            <div class="col-6 mx-auto mt-5 mb-5">
                <div class="card">
                    <div class="card-body">
                        <form action="/users/login" method="POST">
                            <div class="mb-3">
                                <h4 class="card-title text-center">Iniciar Sesión</h4>
                                <label for="exampleInputEmail1" class="form-label">Correo electrónico</label>
                                <input type="email" class="form-control <%= locals.errors && errors.correo ? 'is-invalid' : null%>" id="exampleInputEmail1"
                                    aria-describedby="emailHelp" name="email" value="<%= locals.oldData ? oldData.email : null%>">
                                    <% if (locals.errors && errors.email)  { %>
                                        <div class="text-danger">
                                                <%= errors.email.msg%> 
                                        </div>
                                        <% } %>
                            </div>
                            <div class="mb-3">
                                <label for="exampleInputPassword1" class="form-label">Contraseña</label>
                                <input type="password" class="form-control <%= locals.errors && errors.password ? 'is-invalid' : null%>" id="exampleInputPassword1" name="password">
                                <% if (locals.errors && errors.password) { %>
                                    <div class="text-danger">
                                            <%= errors.password.msg %>
                                    </div>
                                    <% } %>
                                <div id="emailHelp" class="form-text text-center"><a href="">¿Olvidaste la contraseña?</a></div>
                            </div>
                            <div class="mb-3 form-check">
                                <input type="checkbox" class="form-check-input" id="exampleCheck1" name="recordar">
                                <label class="form-check-label" for="exampleCheck1">Recordarme</label>
                            </div>
                            <button type="submit" class="btn btn-primary">Continuar</button>
                            <p>Al continuar, aceptas las <a href="">Condiciones de Uso</a> y
                                el <a href="">Aviso de Privacidad</a> de Electodo</p>
                            <div class="btn-redes">
                                <button type="submit" class="btn btn-primary">
                                    <i class="fab fa-google"></i> Continuar con Google
                                </button>
                                <button type="submit" class="btn btn-primary">
                                    <i class="fab fa-facebook-f"></i> Continuar con Facebook
                                </button>
                                <button type="submit" class="btn btn-primary">
                                    <i class="fab fa-apple"></i> Continuar con Apple
                                </button>
                            </div>
                            <div class="registro">
                                <p>
                                    ¿No tenés cuenta? <a href="/users/register">Registrate.</a>
                                </p>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>
    <%- include('../partials/footer')  %>
    <%- include('../partials/scripts')  %>
</body>

</html>
