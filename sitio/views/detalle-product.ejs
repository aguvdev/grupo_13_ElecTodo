<!DOCTYPE html>
<html lang="es">
    <%- include("./partials/head") %> 
<body>
    <%- include("./partials/header") %>
    <main class="detalle">
        <div class="menu">
            <div class="menu1"><!-- encierro fotos titulo precio logos sumar carrito envios p y h1 -->

                <section class="img-auricular"><!-- encierro solo las fotos -->
                    
                        <img id="img-principal" class="img-fluid" src="/img/<%= producto.images[0] %>" alt="imagen auris">
                        

                        <div class="galeria-auris">
                            <% producto.images.forEach((imagen,index) => { %>
                            <div class="auris">
                                    <img onclick="document.getElementById('img-principal').setAttribute('src','/img/<%=imagen%>')" src="/img/<%= imagen %>" alt="">
                            </div>
                            <% }) %>

                        </div>
                    
                </section>        
                


                <div class="menulado"><!-- encierro logos h1 p precios sumar carrito envios -->
                    
                        <div class="titulo">
                            <h1><%= producto.name %> </h1>  
                        </div>
        
                        <div class="model">
                            <h1> <%= producto.model %> </h1>
                            <h4>Categoria de: <%= producto.category %></h4>
                        </div>
                
                        <div class="precio">
                            <h4> $<%= producto.price %> <br> Precio Lista</h4>
                            <a href="/"><img src="/img/electodo log.png" alt="electodo"></a>
                        </div>
        
                        <div class="discount">
                            <h4>pagando con efectivo un: <br> <%= producto.discount %>%  de descuento</h4>
                            <h4 class="text-primary">$<%= (producto.price - (producto.discount * producto.price /100).toFixed(0)) %> %OFF</h4>
                        </div>
                
                        <div class="garantia-envios">
                            <i class="fad fa-shield-alt"></i> <span>Garantia - 12 meses</span>
                            <p></p>
                            <i class="fas fa-truck"></i> <span>Envios a todo el pais</span>
                        </div> 

                        <% if (locals.userLogin && locals.userLogin.rol === "admin") { %>
                            <div class="botones">  
                                <div >                  
                                    <a class="editor" href="/product/edit/<%= producto.id %>" >Editar</a>                  
                                </div>
                                <div class="botoncito">
                                    <form action="/product/remove/<%= producto.id %>?_method=DELETE" method="POST">
                                        <button class="btn btn-primary btn-danger">Eliminar</button>
                                      </form>
                                </div>
                            </div>
                         
                        <% } %> 
                        
                        <div class="botones">
                            <div class="carrito">
                                <a href="/cart"><span>sumar al carrito</span></a>
                            </div>                      
                        </div>

                    
                </div>
            </div>
        </div>
    
    </div>   
    <div class="contenedor-especificaciones">
        <div class="contenedor-caracteristicas">

                <h3> CARACTERISTICAS GENERALES </h3>
                <div class="caracteristicas-contenido">
                    <h4><%= producto.description %></h4>               
                </div>
            </div>              
                      
    </div>

    <!-- productos relacionado -->
    <section class="relacionados">
        <h3>PRODUCTOS RELACIONADOS</h3>
        <p></p>
        <div class="galeria-mouse">
            <% relacionados.forEach(relacionado => { %>
                <div class="mouse">
                    <img src="/img/<%= relacionado.image %>" alt="mouse">
                    <h4><%= relacionado.titulo %></h4>
                    <span>$<%= relacionado.price %></span>
                </div>
            <% }) %>
        </div>
    </section>     
    
    </main>
    <%- include("./partials/footer") %>
    <%- include("./partials/scripts") %>
</body>
</html>